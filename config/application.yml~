defaults: &defaults
  EMAIL: example@example.com

development:
  <<: *defaults
  KEY: development
  SERVER: thin

test:
  <<: *defaults
  KEY: test

staging:
  <<: *defaults
  KEY: staging
  SERVER: webrick
production:
  <<: *defaults
  KEY: production
